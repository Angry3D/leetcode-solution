const utils = require('../utils');

// const height = [1, 8, 6, 2, 5, 4, 8, 3, 7];
const height = [1, 3, 2, 5, 25, 24, 5];

// utils.test('solution1', height)
// utils.test('solution2', height)
utils.test('solution3', height);
