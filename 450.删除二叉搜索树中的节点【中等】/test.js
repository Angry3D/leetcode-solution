const utils = require('../utils');
const { createBinaryTree } = require('../utils/tree');

// utils.test('solution1', createBinaryTree([5, 3, 6, 2, 4, null, 7]), 3);
utils.test('solution1', createBinaryTree([5, 3, 6, 2, 4, null, 7]), 5);
